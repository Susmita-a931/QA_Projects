{
  "name": "shopease-web-qa",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright automation suite for the ShopEase demo e-commerce website.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:smoke": "playwright test --grep @smoke",
    "report": "playwright show-report",
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "@types/node": "^24.10.1",
    "typescript": "^5.9.3"
  }
}
